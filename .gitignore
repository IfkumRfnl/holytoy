# Big binaries / VM state — never commit
images
images/
out
out/
# Reference clone of TempleOS source (managed by a parallel agent)
vendor/
__pycache__/
/images
/out
# Local-only workloads (licensed material; never commit anything under it)
/local
